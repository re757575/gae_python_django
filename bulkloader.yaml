# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Customers
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: clientAddress
      external_name: clientAddress
      # Type: String Stats: 152 properties of this type in this kind.

    - property: clientName
      external_name: clientName
      # Type: String Stats: 152 properties of this type in this kind.

    - property: clientTel
      external_name: clientTel
      # Type: String Stats: 152 properties of this type in this kind.

    - property: createDateTime
      external_name: createDateTime
      # Type: Date/Time Stats: 152 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%d %H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%d %H:%M:%S')

    - property: createOperatorAccount
      external_name: createOperatorAccount
      # Type: User Stats: 152 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: createTimeStamp
      external_name: createTimeStamp
      # Type: Float Stats: 152 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: note
      external_name: note
      # Type: Text Stats: 152 properties of this type in this kind.
      import_transform: db.Text

    - property: status
      external_name: status
      # Type: Boolean Stats: 152 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: type
      external_name: type
      # Type: Integer Stats: 152 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: _AhAdminXsrfToken_
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: xsrf_token
      external_name: xsrf_token
      # Type: String Stats: 1 properties of this type in this kind.
